default: &default
  adapter: postgresql
  host: <%= ENV['PGHOST'] %>
  username: <%= ENV['PGUSER'] %>
  password: <%= ENV['PGPASSWORD'] %>
  database: <%= ENV['PGDATABASE'] %>
  
development:
  <<: *default
  database: uren-dev

test:
  <<: *default
  database: uren-test

production:
  <<: *default